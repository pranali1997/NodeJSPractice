var data=require('./menu.json');

function hotel(){
    // console.log(data.PERSON[0].nisha_details.name);
    // console.log(data.PERSON[0].pranu_details.name);
    // console.log(data.PERSON[1].kajal_details.hobby);
    // var data = Object.keys(data);
   // console.log(data)for(let i of result){

    // for(let i of data.PERSON[0].nisha_details){
      // var data = Object.keys(i);
     //console.log(data)
     var dah = data.PERSON[0].nisha_details;
     console.log(dah)
     /*
     for(let i of result){
    console.log("i is: ",i)
    console.log("key is: ",Object.keys(i));
    console.log("value is: ",Object.keys(i).map(key => i[key])) // Object.values can be used as well in newer versions.
     */

    }



hotel()